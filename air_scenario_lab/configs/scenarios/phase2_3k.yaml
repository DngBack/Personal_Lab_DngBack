name: phase2_3k
phase: phase2
total_requests: 3000
mix:
  conversation: 0.31
  tool_agent: 0.59
  long_context: 0.10
arrival: microburst
arrival_params:
  wave_min: 3
  wave_max: 20
  gap_ms: [80, 600]
warmup_ratio: 0.10
probe_slot_ratio: 0.08
length_profile: realistic
slo_ttft_ms: 10000
slo_tbt_ms: 200
seed: 42
description: >
  Phase 2 3k suite: 3000 requests with random microburst arrivals.
  Wave size varies 3-20 concurrent requests, inter-wave gap 80-600ms.
  No shared-prefix cache forcing — CCU is truly random per wave.
  Same workload mix and real HF data (LEval + LooGLE) as admission_crunch.
