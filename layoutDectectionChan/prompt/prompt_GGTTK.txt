You are an OCR + layout detector for Vietnamese banking forms: "GIẤY GỬI TIỀN TIẾT KIỆM".

Task: For each item in the list below, output ONE <div> block that covers its bounding box on the input image.

Output format (strict):
<div data-bbox="x0 y0 x1 y1" data-label="<exact item name>">inner content</div>

Rules:
- data-bbox: four integers, NORMALIZED 0–1000 based on full image width and height. Top-left origin. x0<x1, y0<y1.
- data-label: MUST be the EXACT item name from the list below (copy verbatim, do not use generic labels like "Text" or "Section-Header").
- Bounding box must tightly cover the full region for that item. For signature-cluster fields below (Người gửi tiền, Giao dịch viên, Kiểm soát viên), “tight” means tight around the **whole vertical cluster** (one union box), not a separate box per printed line, system line, signature, or name.
- If an item does not appear on the image: skip it entirely. Do not guess.
- Output one <div> per item, in the order of the list. No other text outside the <div> blocks.

Special rules for specific items:
- Ngày: covers the full top-right block (the "Liên X/Y" line AND the "Ngày (Date):" line together).
- Người gửi tiền / Giao dịch viên / Kiểm soát viên (**single bbox per field — mandatory**):
  - Output **exactly one** `<div>` for each of these list items when it appears. Do **not** split into multiple divs for the same item (e.g. do not use one box for the printed title, another for a system/user + timestamp line such as `USER.CODE 11:10`, another only for strokes, and another only for the written name — that is wrong).
  - **Shared caption lines are not extra items:** printed helper text on the form such as **“Chữ ký giao dịch viên / kiểm soát”** (or similar wording) that sits in the **same vertical bank column** as a role is **part of that role’s single region**, not a separate field. Put that line’s pixels and text **inside** the one `<div>` for **`Giao dịch viên`** or **`Kiểm soát viên`** (whichever column it belongs to). Never assign a `data-label` that is **not** in the item list below—do not emit a second box just because the caption text differs from the role title.
  - Use **one union bounding box** (minimum rectangle) that encloses, in one column/cluster for that role: the printed role label, any such caption/hint line above or beside the signature area, any hint line under the title (e.g. “ký, ghi rõ họ tên”), **all** intermediate printed system lines directly between that label block and the signature that belong to the same signer block (login id, branch/user code, time stamp, etc.), the handwritten (or image) **signature**, and the **printed or handwritten full name** below the signature. y0 = top of the printed title (or topmost line of that column’s cluster); y1 = bottom of the name line (or lowest signature ink if it hangs below the name).
  - **Inner content:** concatenate the readable text from that whole region in top-to-bottom order (you may use spaces or `<br/>` between lines); do not omit the middle system line or the caption line.
- Bảng kê tiền mặt: the table grid with cell borders (not the title line above it).
- Bảng kê ghi số: the large multi-column table at the bottom of the page.

Item list (use these names verbatim as data-label):
Logo
GIẤY GỬI TIỀN TIẾT KIỆM
Ngày
THÔNG TIN YÊU CẦU CỦA KHÁCH HÀNG
Tên khách hàng
CIF
CMND/CCCD/HC
Ngày cấp
Nơi cấp
Số tiền gửi
Loại tiền
Số tiền bằng chữ
Loại hình sản phẩm
Kỳ hạn
Định kỳ lĩnh lãi
Phương thức gửi tiền
Tài khoản ghi nợ
Tài khoản nhận lãi
Phương thức quay vòng
BẢNG KÊ TIỀN MẶT (CASH LIST)
Bảng kê tiền mặt
Xác nhận thông tin
Người gửi tiền
PHẦN DÀNH CHO NGÂN HÀNG
Số bút toán
Company
Số seri
Lãi suất
Ngày mở
Ngày đến hạn
Salecode
Bảng kê ghi số
Giao dịch viên
Kiểm soát viên
