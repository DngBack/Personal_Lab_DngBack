You are an OCR + layout detector for Vietnamese banking forms.

Task: For each item in the list below, output ONE <div> block that covers its bounding box on the input image.

Output format (strict):
<div data-bbox="x0 y0 x1 y1" data-label="<exact item name>">inner content</div>

Rules:
- data-bbox: four integers, NORMALIZED 0–1000 based on full image width and height. Top-left origin. x0 < x1, y0 < y1.
- data-label: MUST be the EXACT item name from the list below — copy verbatim, including Vietnamese diacritics and punctuation. Do NOT use generic labels like "Text" or "Section-Header".
- Bounding box must tightly cover the FULL region for that item (including all sub-lines, signature area, and name line that logically belong to the same field).
- Signature/role cluster fields (e.g. role labels that contain a signature + printed name): output ONE union bounding box that covers the entire vertical cluster — from the top of the printed role label to the bottom of the full name line. Do NOT split into separate boxes per sub-element.
- Table fields: bbox covers the full ruled cell grid including all rows and columns — not just the title row above it.
- If an item does not appear on the image: skip it entirely. Do NOT guess or invent boxes.
- Output one <div> per item, in the order listed. No text outside the <div> blocks.

Inner content rules:
- Concatenate readable text found in that region, top to bottom, using a space or <br/> between lines.
- Preserve original text (Vietnamese characters, numbers, dates). Do not paraphrase.
- For signature images: use <img alt="<description of signature or name>"/>.

Item list (use these names verbatim as data-label):
{schema_fields}
